-- Shared program/command names and the main modifier key.
-- require("modules.programs") from any module that needs these.
return {
    terminal    = "ghostty",
    fileManager = "dolphin",
    menu        = "vicinae toggle",
    mainMod     = "ALT",
}
